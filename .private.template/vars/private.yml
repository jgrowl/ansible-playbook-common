common_iface: ${ansible_default_ipv4.alias}
common_address: ${ansible_default_ipv4.address}
common_netmask: ${ansible_default_ipv4.netmask}
common_network: ${ansible_default_ipv4.network}
common_broadcast: 192.168.1.255 # Need to figure out how to make this machine specific from ansible
common_gateway: ${ansible_default_ipv4.gateway}
common_dns_name_servers: 8.8.8.8 8.8.4.4 
common_dns_search: example.com
